import 'package:flutter/material.dart';

class ReglazeYourGlassesScreen extends StatelessWidget {
  const ReglazeYourGlassesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Reglaze Your Glasses'),
      ),
      body: const Center(
        child: Text('Reglaze Your Glasses service information goes here.'),
      ),
    );
  }
}
